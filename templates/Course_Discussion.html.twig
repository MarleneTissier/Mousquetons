{% extends 'base.html.twig' %}
{% block content %}

    <main>
        <div class="container-fluid">
            <ul class="row ariane">
                <li class="ArticleLigne"><a  class="ArianeLien"  class="ArianeLien" {{ path('Home') }}>Acceuil</a></li>
                <li><a class="ArianeLien" href={{ path('Course') }}>Parcours</a> </li>
                <li><span class="ArianeSpan" >/ Discussions</span></li>
            </ul>
        </div>

        <!--article France-->
        <article  class="article">
            <div class="row align-items-center text-center text-md-left p-4 p-md-0 bandeauFirst articleDiv"
                 style="background-image: url({{ asset("img/france_parcour.jpg") }});">
                <div class="d-none d-md-block col-2 icon articleDiv">
                    <img src="{{ asset('img/france_parcour.jpg') }}" alt="" class="img-fluid">
                </div>
            </div>
            <ul class="ArticleLiens">
                <li class="ArticleLigne">
                    <ul class="row menu container-fluid justify-content-between align-items-center m-0 ArticleLiens">
                        <li class="col-md-6 text-center text-md-left">
                            <h2>Les parcours en France</h2>
                        </li>
                        <li class="d-none d-md-block col-2 text-center">
                            <span class="ArticleSpan">Auteur</span>
                        </li>
                        <li class="d-none d-md-block col-1 text-center">
                            <span class="ArticleSpan">nombres de réponses</span>
                        </li>
                        <li class="d-none d-md-block col-2 text-center">
                            <span class="ArticleSpan">date d'ajour du post</span>
                        </li>
                    </ul>
                </li>
                <!-- for pour récup les 3 derniers post -->
                {% for parcourFrance in parcoursFrance %}
                    <li class="ArticleLigne">
                        <a class="ArticleLien" href="{{ path('post') }}" >
                            <ul class="row menu container-fluid justify-content-between text-align-center align-items-center m-0 ArticleLiens">
                                <li class="col-md-6 text-center text-md-left">
                                    <h4>{{parcourFrance.name}}</h4>
                                    <p class="ArticleParagraph" >{{ parcourFrance.FirstPost }}</p>
                                </li>
                                <li class="d-none d-md-block col-1 text-center">
                                    <span class="ArticleSpan">{{ parcourFrance.user.username }}</span>
                                </li>
                                <li class="d-none d-md-block col-1 text-center">
                                    <span class="ArticleSpan">{{ parcourFrance.place.name }}</span>
                                </li>
                                <li class="d-none d-md-block col-1 text-center">
                                    <span class="ArticleSpan">{{ parcourFrance.NmbPos }}</span>
                                </li>
                                <li class="d-none d-md-block col-2 text-center">
                                    <span class="ArticleSpan">{{ parcourFrance.date|date('d-M-Y') }}</span>
                                </li>
                            </ul>
                        </a>
                    </li>
                {% endfor %}

            </ul>
            <div class="text-center text-md-right articleDiv">
                <a class="ArticleLien"  href={{ path('CourseDiscussion') }}>Voir plus</a>
            </div>

        </article>

        <!--article Espagne-->
        <article  class="article">
            <div class="row align-items-center text-center text-md-left p-4 p-md-0 bandeauFirst articleDiv"
                 style="background-image: url({{ asset("img/france_parcour.jpg") }});">
                <div class="d-none d-md-block col-2 icon articleDiv">
                    <img src="{{ asset('img/france_parcour.jpg') }}" alt="" class="img-fluid">
                </div>
            </div>
            <ul class="ArticleLiens">
                <li class="ArticleLigne">
                    <ul class="row menu container-fluid justify-content-between align-items-center m-0 ArticleLiens">
                        <li class="col-md-6 text-center text-md-left">
                            <h2>Les parcours en Espagne</h2>
                        </li>
                        <li class="d-none d-md-block col-2 text-center">
                            <span class="ArticleSpan">Auteur</span>
                        </li>
                        <li class="d-none d-md-block col-1 text-center">
                            <span class="ArticleSpan">nombres de réponses</span>
                        </li>
                        <li class="d-none d-md-block col-2 text-center">
                            <span class="ArticleSpan">date d'ajour du post</span>
                        </li>
                    </ul>
                </li>
                <!-- for pour récup les 3 derniers post -->
                {% for parcourEspagne in parcoursEspagne %}
                    <li class="ArticleLigne">
                        <a class="ArticleLien" href="{{ path('post') }}" >
                            <ul class="row menu container-fluid justify-content-between text-align-center align-items-center m-0 ArticleLiens">
                                <li class="col-md-6 text-center text-md-left">
                                    <h4>{{parcourEspagne.name}}</h4>
                                    <p class="ArticleParagraph" >{{ parcourEspagne.FirstPost }}</p>
                                </li>
                                <li class="d-none d-md-block col-1 text-center">
                                    <span class="ArticleSpan">{{ parcourEspagne.user.username }}</span>
                                </li>
                                <li class="d-none d-md-block col-1 text-center">
                                    <span class="ArticleSpan">{{ parcourEspagne.place.name }}</span>
                                </li>
                                <li class="d-none d-md-block col-1 text-center">
                                    <span class="ArticleSpan">{{ parcourEspagne.NmbPos }}</span>
                                </li>
                                <li class="d-none d-md-block col-2 text-center">
                                    <span class="ArticleSpan">{{ parcourEspagne.date|date('d-M-Y') }}</span>
                                </li>
                            </ul>
                        </a>
                    </li>
                {% endfor %}
            </ul>
            <div class="text-center text-md-right articleDiv">
                <a class="ArticleLien" href="{{ path('CourseDiscussion') }}">Voir plus</a>
            </div>

        </article>

        <!--article Europe-->
        <article  class="article">
            <div class="row align-items-center text-center text-md-left p-4 p-md-0 bandeauFirst articleDiv"
                 style="background-image: url({{ asset("img/france_parcour.jpg") }});">
                <div class="d-none d-md-block col-2 icon articleDiv">
                    <img src="{{ asset('img/france_parcour.jpg') }}" alt="" class="img-fluid">
                </div>
            </div>
            <ul class="ArticleLiens">
                <li class="ArticleLigne">
                    <ul class="row menu container-fluid justify-content-between align-items-center m-0 ArticleLiens">
                        <li class="col-md-6 text-center text-md-left">
                            <h2>Les parcours en Europe</h2>
                        </li>
                        <li class="d-none d-md-block col-2 text-center">
                            <span class="ArticleSpan">Auteur</span>
                        </li>
                        <li class="d-none d-md-block col-1 text-center">
                            <span class="ArticleSpan">nombres de réponses</span>
                        </li>
                        <li class="d-none d-md-block col-2 text-center">
                            <span class="ArticleSpan">date d'ajour du post</span>
                        </li>
                    </ul>
                </li>
                <!-- for pour récup les 3 derniers post -->
                {% for parcourEurope in parcoursEurope %}
                    <li class="ArticleLigne">
                        <a class="ArticleLien" href="{{ path('post') }}" >
                            <ul class="row menu container-fluid justify-content-between text-align-center align-items-center m-0 ArticleLiens">
                                <li class="col-md-6 text-center text-md-left">
                                    <h4>{{parcourEurope.name}}</h4>
                                    <p class="ArticleParagraph" >{{ parcourEurope.FirstPost }}</p>
                                </li>
                                <li class="d-none d-md-block col-1 text-center">
                                    <span class="ArticleSpan">{{ parcourEurope.user.username }}</span>
                                </li>
                                <li class="d-none d-md-block col-1 text-center">
                                    <span class="ArticleSpan">{{ parcourEurope.place.name }}</span>
                                </li>
                                <li class="d-none d-md-block col-1 text-center">
                                    <span class="ArticleSpan">{{ parcourEurope.NmbPos }}</span>
                                </li>
                                <li class="d-none d-md-block col-2 text-center">
                                    <span class="ArticleSpan">{{ parcourEurope.date|date('d-M-Y') }}</span>
                                </li>
                            </ul>
                        </a>
                    </li>
                {% endfor %}
            </ul>
            <div class="text-center text-md-right articleDiv">
                <a class="ArticleLien" href="{{ path('CourseDiscussion') }}">Voir plus</a>
            </div>

        </article>

        <!--article Monde-->
        <article  class="article">
            <div class="row align-items-center text-center text-md-left p-4 p-md-0 bandeauFirst articleDiv"
                 style="background-image: url({{ asset("img/france_parcour.jpg") }});">
                <div class="d-none d-md-block col-2 icon articleDiv">
                    <img src="{{ asset('img/france_parcour.jpg') }}" alt="" class="img-fluid">
                </div>
            </div>
            <ul class="ArticleLiens">
                <li class="ArticleLigne">
                    <ul class="row menu container-fluid justify-content-between align-items-center m-0 ArticleLiens">
                        <li class="col-md-6 text-center text-md-left">
                            <h2>Les parcours dans le monde</h2>
                        </li>
                        <li class="d-none d-md-block col-2 text-center">
                            <span class="ArticleSpan">Auteur</span>
                        </li>
                        <li class="d-none d-md-block col-1 text-center">
                            <span class="ArticleSpan">nombres de réponses</span>
                        </li>
                        <li class="d-none d-md-block col-2 text-center">
                            <span class="ArticleSpan">date d'ajour du post</span>
                        </li>
                    </ul>
                </li>
                <!-- for pour récup les 3 derniers post -->
                {% for parcourMonde in parcoursMonde %}
                    <li class="ArticleLigne">
                        <a class="ArticleLien" href="{{ path('post') }}" >
                            <ul class="row menu container-fluid justify-content-between text-align-center align-items-center m-0 ArticleLiens">
                                <li class="col-md-6 text-center text-md-left">
                                    <h4>{{parcourMonde.name}}</h4>
                                    <p class="ArticleParagraph" >{{ parcourMonde.FirstPost }}</p>
                                </li>
                                <li class="d-none d-md-block col-1 text-center">
                                    <span class="ArticleSpan">{{ parcourMonde.user.username }}</span>
                                </li>
                                <li class="d-none d-md-block col-1 text-center">
                                    <span class="ArticleSpan">{{ parcourMonde.place.name }}</span>
                                </li>
                                <li class="d-none d-md-block col-1 text-center">
                                    <span class="ArticleSpan">{{ parcourMonde.NmbPos }}</span>
                                </li>
                                <li class="d-none d-md-block col-2 text-center">
                                    <span class="ArticleSpan">{{ parcourMonde.date|date('d-M-Y') }}</span>
                                </li>
                            </ul>
                        </a>
                    </li>
                {% endfor %}
            </ul>
            <div class="text-center text-md-right articleDiv">
                <a class="ArticleLien" href="{{ path('CourseDiscussion') }}">Voir plus</a>
            </div>

        </article>

    </main>



{% endblock %}